"use strict";
var Agent00;
(function (Agent00) {
    function getCommand(_state) {
        let command = { module: "", method: "" };
        return command;
    }
    Agent00.getCommand = getCommand;
})(Agent00 || (Agent00 = {}));
